지금 코드 로직 자체는 방향(서→동)도 맞고, 출력도 맞게 나와. “c가 나오면 0 찍고 그 뒤로 1,2,3…” 이런 방식이 딱 정답 패턴임.

다만 코테 관점에서 고칠 만한 포인트(에러 가능/스타일/성능)는 있어.

출력은 문자열 말고 정수로 처리하는 게 보통 더 깔끔함
지금은 tmp에 "0", "-1" 같은 문자열을 넣고 join으로 출력하는데, 정수로 넣고 마지막에 map(str, ...) 하는 게 정석이야.

"c" not in grid 분기 없어도 됨
한 줄을 왼쪽부터 보면서 last_c(마지막 구름 위치)만 기억하면, 구름이 아예 없는 줄도 자동으로 -1로 처리돼서 분기가 필요 없어져.

cnt 방식도 OK지만 “마지막 구름 위치” 방식이 더 직관적임
last = -1 두고,

현재가 'c'면 last = j, 출력 0

'.'이면 last가 -1이면 -1, 아니면 j-last